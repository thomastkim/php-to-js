<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Blade View
    |--------------------------------------------------------------------------
    |
    | If left empty, the JavaScript variables will be appended 
    | to the end of your HTML head. Otherwise, the variables
    | will be added to the start of the specified view.
    |
    */
    'view' => '',

    /*
    |--------------------------------------------------------------------------
    | JavaScript Namespace
    |--------------------------------------------------------------------------
    |
    | The window of your browser is the global object. As such, the default
    | 'window' namespace allows the JavaScript variables to be available
    | globally. To prevent polluting the global namespace and potential
    | name clashings, it is recommended that you change this value.
    |
    */
    'namespace' => 'window'

];